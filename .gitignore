.vercel
dist/
*.mp4
*.gif
node_modules/
.pnpm-store/
discover/node_modules/
discover/.next/
discover/tsconfig.tsbuildinfo

# Supabase CLI transient state (contains project ref / pooler connection URLs —
# MANDATORY to keep out of the public repo, do not commit)
supabase/.temp/

# Local test-run artifacts
test-results/.last-run.json
discover/test-results/.last-run.json

# Environment files — never commit secrets
.env
.env.local
.env.*.local

# Local runtime / dev artifacts and agent run logs — never publish
data/
.codex/team-runs/
.DS_Store

# Local content persistence with PII-like sample data — keep out of the repo
discover/data/
